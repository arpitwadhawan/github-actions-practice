name: First workflow
on: push

jobs:
 my_job: 
  name: Greet
  runs-on: ubuntu-latest
  steps:
   - name: Checkout Code
     uses: actions/checkout@v4
   
   - name: Print Code
     run: echo "Hello from Github Action!"

